import * as React from 'react'


interface Props {
  message: string;
  show: boolean;
  onClose: () => void;
}

export const NotifictionComponent: React.FC<Props> = (props) => {
  return (
    
  )
}